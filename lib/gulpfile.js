'use strict';

const gulp = require('gulp');
const fs = require('fs');

gulp.task('dist', () => {
	console.log('run-dist');
});

gulp.task('publish', () => {
	console.log('run-publish');
});

gulp.task('compile', () => {
	console.log('run-compile');
});